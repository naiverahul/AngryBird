package vemy.profiles;

public class loaded_profile extends default_player {
    /*
     * This class is the loaded profile class. 
     * The files are loaded from the profile_db folder.
     * It should take the values from a json or a text file and set the values of the variables to the values in the file.
     * It should have methods to load the values and save the values to a file.
     */
}
