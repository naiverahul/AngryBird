package vemy.profiles;

public class new_profile extends default_player {
    /*
     * This class creates a new profile for the player. It should be able to create a file (text or json) which will hold the information.
     * File must be saved in the profile_db folder.
     * It should have methods to take all values of the variable and set them to the values in the file.
     */
}
